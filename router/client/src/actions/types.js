export const SIGN_IN = "SIGN_IN";
export const SIGN_OUT = "SIGN_OUT";

// this avoids a very common where a type in the constant name
// doesn't raise an error msg (instead its a logic error that
// is much harder to debug).
// + reusability is better.
